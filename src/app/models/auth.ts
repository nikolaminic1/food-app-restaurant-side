export interface User {
  id: number;
  name: string;
  email: string;
}

export interface Address {
  streetName: string;
  buildingNumber: string;
  flatNumber: string;
  zipCode: string;
  cityName: string;
}
